local arcane_card_effects_module = {}
-- general letter ideas
-- the abilities differ from type to type but same letters have similar effects
-- except for item??? not sure yet
-- A:
-- B:
-- C:
-- D:
-- E:
-- F:
-- G:
-- H:
-- I:
-- J:
-- K:
-- L:
-- M:
-- O:
-- P:
-- Q:
-- R:
-- S:
-- T:
-- U:
-- V:
-- W:
-- X:
-- Y:
-- Z:

arcane_card_effects_module.A = {
    type = "arcane",
    letter = "A",
    when_drawn_num = {},
    whenDrawn = function(card)


        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.B = {
    type = "arcane",
    letter = "B",
    when_drawn_num = {},
    whenDrawn = function(card)

        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.C = {
    type = "arcane",
    letter = "C",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.D = {
    type = "arcane",
    letter = "D",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.E = {
    type = "arcane",
    letter = "E",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.F = {
    type = "arcane",
    letter = "F",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.G = {
    type = "arcane",
    letter = "G",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.H = {
    type = "arcane",
    letter = "H",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.I = {
    type = "arcane",
    letter = "I",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.J = {
    type = "arcane",
    letter = "J",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.K = {
    type = "arcane",
    letter = "K",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.L = {
    type = "arcane",
    letter = "L",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.M = {
    type = "arcane",
    letter = "M",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.N = {
    type = "arcane",
    letter = "N",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.O = {
    type = "arcane",
    letter = "O",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.P = {
    type = "arcane",
    letter = "P",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.Q = {
    type = "arcane",
    letter = "Q",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.R = {
    type = "arcane",
    letter = "R",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = true,
    retain_num = 1,
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.R.when_retained_description = "Gives +"..arcane_card_effects_module.R.retain_num.." arcane. Increased by 1 for every turn retained"
arcane_card_effects_module.R.whenRetained = function(card)
    if not arcane_card_effects_module.G.controller then error "G.controller is nil" end
    arcane_card_effects_module.R.retain_num = arcane_card_effects_module.R.retain_num + 1
    arcane_card_effects_module.R.when_retained_description = "Gives +"..arcane_card_effects_module.R.retain_num.." arcane. Increased by 1 for every turn retained"
    card.updateDescription()
    G.controller.addResource("arcane", arcane_card_effects_module.R.retain_num)
end
arcane_card_effects_module.S = {
    type = "arcane",
    letter = "S",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.T = {
    type = "arcane",
    letter = "T",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.U = {
    type = "arcane",
    letter = "U",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
        G.controller.aoeUpdateHp(-3)
    end,
    when_drawn_description = "+2 arcane, deal 3 dmg to all enemies",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.V = {
    type = "arcane",
    letter = "V",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.W = {
    type = "arcane",
    letter = "W",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.X = {
    type = "arcane",
    letter = "X",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.Y = {
    type = "arcane",
    letter = "Y",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}
arcane_card_effects_module.Z = {
    type = "arcane",
    letter = "Z",
    when_drawn_num = {},
    whenDrawn = function(card)
        G.controller.addResource("arcane", 2)
    end,
    when_drawn_description = "+2 arcane",
    retain = false,
    when_retained = {},
    when_retained_description = "",
    discard = false,
    when_discarded = {},
    when_discarded_description = ""
}

return arcane_card_effects_module